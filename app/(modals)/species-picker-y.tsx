/**
 * G0 PoC Y 案: presentation: 'modal' (`(modals)/_layout.tsx` で指定)。
 *
 * iOS: native Sheet 風の下から上アニメーション。
 * Android: 通常の画面遷移 (presentation: 'modal' は iOS 中心の挙動)。
 *
 * ADR-0024 / Issue #475 Phase G0。
 */
export { default } from '@/src/features/bonsai/SpeciesPickerScreen';
